# Radar2 Map Routing Service Dependencies
# Dependencies for routing service with PostgreSQL backend

# Core Flask service
flask==3.0.0
flask-cors==4.0.0

# PostgreSQL client for pgRouting backend
# Note: psycopg2-binary may need special installation on some systems
# Alternative: brew install postgresql && pip install psycopg2
psycopg2-binary>=2.9.7

# JSON handling for PostgreSQL responses
# (built-in json module is sufficient)

# Development and testing (optional)
pytest>=7.4.0
requests>=2.31.0
